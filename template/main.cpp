#include <bits/stdc++.h>
using namespace std;

#define lli long long int
#define lliv vector<long long int>

int main(void) {
    vector<int> v(10, 5);

    cout << "Hello world!" << endl;

    for (auto i : v) {
        cout << i << endl;
    }

    int input;
    cin >> input;
    cout << input << endl;

    return 0;
}
